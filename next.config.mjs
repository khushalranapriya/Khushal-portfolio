/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
    // Enable if needed for Tailwind v4
    // optimizeCss: true,
    //  appDir: true,
  },
};

export default nextConfig;
